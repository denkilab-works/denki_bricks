name: denki_dto_mapper_hooks

environment:
  sdk: ^3.7.0

dependencies:
  mason: ^0.1.1
  denki_bricks_helper:
    git:
      url: https://github.com/denkilab-works/denki_bricks.git
      path: denki_bricks_helper
      ref: main
# Uncomment this during development to quickly apply changes
# dependency_overrides:
#   denki_bricks_helper:
#     path: ../../denki_bricks_helper
